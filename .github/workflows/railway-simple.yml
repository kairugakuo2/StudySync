name: Deploy Backend to Railway (Simple)

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Deploy to Railway
        run: |
          echo "🚀 Railway will auto-deploy from GitHub connection"
          echo "Make sure Railway is connected to this repo in Railway dashboard"
          echo "Railway watches for changes in the 'backend' directory"


